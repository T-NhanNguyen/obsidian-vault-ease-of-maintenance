pi-tasks extension with `task_plan`/`task_checkpoint` installed at `/Users/nhan/.pi/agent/npm/node_modules/pi-tasks`; its plan-quality gate (MIN_PLAN_QUALITY_SCORE=80) rejects per-step at the first step below 80.	TASK_0001
Git HEAD is `b320263` (Aug 18); the entire comprehension feature exists only as uncommitted working-tree state; `.dev-vault/` and `.pi-tasks/` are gitignored.	TASK_0001
