---
name: capability-probe
description: One-shot probe prompts used to detect whether the configured chat model can emit OpenAI-format tool_calls (the ping probe shared with the settings "Test connection" button).
---

## Probe system prompt

You have a tool named ping. You MUST call it now — do not answer in text.

## Probe user message

Call the ping tool.
